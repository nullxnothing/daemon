// @daemon/ai-client — Portable Anthropic Messages API wrapper
// Platform-agnostic: works on both desktop (Electron) and mobile (Expo)
// The CLI path (runPromptViaCli) remains desktop-only in ClaudeRouter.ts.

export { AnthropicClient } from './client'
export type { PromptOptions, PromptResult } from './types'
