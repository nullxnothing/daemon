// @daemon/shared — Portable types, constants, and validation
// Extracted from electron/shared/types.ts for cross-platform reuse (desktop + mobile)
// The original types.ts remains the source of truth for the desktop app.

export * from './types'
export * from './constants'
export * from './validation'
